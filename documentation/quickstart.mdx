---
title: "Hızlı Başlangıç"
description: "Beş dakikada Payven Sanal POS üzerinden ilk test ödemesini başlatın."
---

Bu rehber, Payven Sanal POS API'sine yapılan **ilk başarılı ödeme** için gereken minimum adımları gösterir. Tüm kod örnekleri test ortamı içindir.

<Note>
**Ön koşul:** [Konsol](https://dashboard.payven.com.tr) üzerinden bir hesabınız ve aktif bir merchant tanımınız olmalıdır.
</Note>

## 1. API anahtarı oluşturun

Konsola giriş yaptıktan sonra **Ayarlar → API Anahtarları → Yeni Anahtar Oluştur** adımıyla bir anahtar üretin. Üretilen değerleri güvenli bir yerde saklayın:

| Bilgi | Açıklama |
|---|---|
| `apiKey` | Public tanımlayıcı (`pvk-` ile başlar). |
| `apiSecret` | Gizli imza anahtarı. **Yalnızca oluşturma anında bir kez gösterilir.** |
| `merchantId` | Ödemeyi gerçekleştirecek merchant'ın kimliği. |

<Warning>
`apiSecret` değerini hiçbir zaman istemci tarafı (tarayıcı, mobil uygulama) kodlarına gömmeyin. Tüm Payven API çağrıları yalnızca **sunucu tarafından** yapılmalıdır.
</Warning>

## 2. İlk isteğinizi atın

Aşağıdaki örnek, **Non-3D bir test ödemesi** başlatır. Tutar **kuruş** cinsindendir — `15000` değeri 150,00 ₺'ye karşılık gelir.

<CodeGroup>

```bash cURL
curl -X POST https://vpos-sandbox.payven.com.tr/api/v1/payments \
  -H "X-API-Key: pvk-test-XXXX" \
  -H "X-API-Secret: YOUR_SECRET" \
  -H "X-Merchant-Id: 3fa85f64-5717-4562-b3fc-2c963f66afa6" \
  -H "Content-Type: application/json" \
  -d '{
    "externalId": "ORDER-1001",
    "amount": 15000,
    "currency": "TRY",
    "installment": 1,
    "card": {
      "holderName": "Test Kullanici",
      "number": "4546711234567894",
      "expireMonth": "12",
      "expireYear": "2030",
      "cvv": "000"
    }
  }'
```

```csharp C#
using System.Net.Http.Json;

var http = new HttpClient { BaseAddress = new Uri("https://vpos-sandbox.payven.com.tr") };
http.DefaultRequestHeaders.Add("X-API-Key", "pvk-test-XXXX");
http.DefaultRequestHeaders.Add("X-API-Secret", "YOUR_SECRET");
http.DefaultRequestHeaders.Add("X-Merchant-Id", "3fa85f64-5717-4562-b3fc-2c963f66afa6");

var response = await http.PostAsJsonAsync("/api/v1/payments", new
{
    externalId = "ORDER-1001",
    amount = 15000,
    currency = "TRY",
    installment = 1,
    card = new
    {
        holderName = "Test Kullanici",
        number = "4546711234567894",
        expireMonth = "12",
        expireYear = "2030",
        cvv = "000"
    }
});

var result = await response.Content.ReadAsStringAsync();
Console.WriteLine(result);
```

```javascript Node.js
const response = await fetch("https://vpos-sandbox.payven.com.tr/api/v1/payments", {
  method: "POST",
  headers: {
    "X-API-Key": "pvk-test-XXXX",
    "X-API-Secret": "YOUR_SECRET",
    "X-Merchant-Id": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    externalId: "ORDER-1001",
    amount: 15000,
    currency: "TRY",
    installment: 1,
    card: {
      holderName: "Test Kullanici",
      number: "4546711234567894",
      expireMonth: "12",
      expireYear: "2030",
      cvv: "000"
    }
  })
});

const data = await response.json();
console.log(data);
```

```go Go
package main

import (
	"bytes"
	"encoding/json"
	"io"
	"net/http"
)

func main() {
	body, _ := json.Marshal(map[string]any{
		"externalId":  "ORDER-1001",
		"amount":      15000,
		"currency":    "TRY",
		"installment": 1,
		"card": map[string]string{
			"holderName":  "Test Kullanici",
			"number":      "4546711234567894",
			"expireMonth": "12",
			"expireYear":  "2030",
			"cvv":         "000",
		},
	})

	req, _ := http.NewRequest("POST",
		"https://vpos-sandbox.payven.com.tr/api/v1/payments",
		bytes.NewReader(body))
	req.Header.Set("X-API-Key", "pvk-test-XXXX")
	req.Header.Set("X-API-Secret", "YOUR_SECRET")
	req.Header.Set("X-Merchant-Id", "3fa85f64-5717-4562-b3fc-2c963f66afa6")
	req.Header.Set("Content-Type", "application/json")

	resp, _ := http.DefaultClient.Do(req)
	defer resp.Body.Close()
	out, _ := io.ReadAll(resp.Body)
	println(string(out))
}
```

```php PHP
<?php
$ch = curl_init("https://vpos-sandbox.payven.com.tr/api/v1/payments");
curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => [
        "X-API-Key: pvk-test-XXXX",
        "X-API-Secret: YOUR_SECRET",
        "X-Merchant-Id: 3fa85f64-5717-4562-b3fc-2c963f66afa6",
        "Content-Type: application/json",
    ],
    CURLOPT_POSTFIELDS => json_encode([
        "externalId"  => "ORDER-1001",
        "amount"      => 15000,
        "currency"    => "TRY",
        "installment" => 1,
        "card" => [
            "holderName"  => "Test Kullanici",
            "number"      => "4546711234567894",
            "expireMonth" => "12",
            "expireYear"  => "2030",
            "cvv"         => "000",
        ],
    ]),
]);
echo curl_exec($ch);
```

```python Python
import requests

response = requests.post(
    "https://vpos-sandbox.payven.com.tr/api/v1/payments",
    headers={
        "X-API-Key": "pvk-test-XXXX",
        "X-API-Secret": "YOUR_SECRET",
        "X-Merchant-Id": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
    },
    json={
        "externalId": "ORDER-1001",
        "amount": 15000,
        "currency": "TRY",
        "installment": 1,
        "card": {
            "holderName": "Test Kullanici",
            "number": "4546711234567894",
            "expireMonth": "12",
            "expireYear": "2030",
            "cvv": "000",
        },
    },
)
print(response.json())
```

</CodeGroup>

## 3. Yanıtı yorumlayın

Başarılı bir Non-3D ödemenin yanıtı şu yapıdadır:

```json
{
  "isSuccess": true,
  "message": "Başarılı.",
  "code": "200",
  "data": {
    "id": "8e3f5c12-9a7b-4c8d-bc4e-2c963f66afa6",
    "externalId": "ORDER-1001",
    "status": "Success",
    "amount": 15000,
    "currency": "TRY",
    "installment": 1,
    "authCode": "123456",
    "hostReference": "PAYVEN-REF-789",
    "connector": {
      "code": "GarantiVPOS",
      "responseCode": "00",
      "responseMessage": "Onaylandı"
    },
    "createdAt": "2026-05-03T12:34:56Z"
  }
}
```

İstediğiniz alanın anlamı için [API Yanıt Formatı](/documentation/concepts/response-format) sayfasına bakın.

## 4. Webhook ile sonucu doğrulayın

Asenkron işlem sonuçlarını (3D Secure, capture, iade, mutabakat olayları) yakalamak için bir webhook endpoint'i kaydedin. Detay: [Webhook Genel Bakış](/sanal-pos/webhooks/overview).

```bash
curl -X POST https://vpos-sandbox.payven.com.tr/api/v1/webhooks \
  -H "X-API-Key: pvk-test-XXXX" \
  -H "X-API-Secret: YOUR_SECRET" \
  -H "X-Merchant-Id: 3fa85f64-5717-4562-b3fc-2c963f66afa6" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://example.com/webhooks/payven",
    "events": ["payment.succeeded", "payment.failed", "refund.succeeded"]
  }'
```

Yanıttaki `secret` değerini saklayın — bu değerle gelen webhook isteklerinin imzasını doğrulayacaksınız.

## Sonraki adımlar

<CardGroup cols={2}>
  <Card title="3D Secure ödeme" icon="shield-check" href="/sanal-pos/payments/3d-secure">
    Müşteriyi banka doğrulama sayfasına yönlendiren tam akış.
  </Card>
  <Card title="Test kartları" icon="id-card" href="/sanal-pos/test/test-cards">
    Sandbox ortamında kullanılabilir tüm test kartlarının listesi.
  </Card>
  <Card title="Hata yönetimi" icon="triangle-exclamation" href="/documentation/concepts/errors">
    Hata kodları, retry stratejisi ve idempotency kullanımı.
  </Card>
  <Card title="Production geçişi" icon="rocket" href="/documentation/security/go-live-checklist">
    Canlıya geçmeden önce tamamlanması gereken kontrol listesi.
  </Card>
</CardGroup>
