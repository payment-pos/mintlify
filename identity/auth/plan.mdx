---
title: "Plan (/me/plan)"
description: "Organizasyonunuzun atanmış planı ve plan'a dahil feature'lar."
---

`/me/plan` endpoint'i; aktif tenant'ınızın atanmış planını ve plana dahil feature'ları döner. Sadece okunabilir (read-only); plan değişikliği konsoldan veya satış ekibinden talep edilir.

## Endpoint

```http
GET /api/v1/me/plan
```

Bearer access token gerektirir.

## İstek

```bash
curl https://identity.payven.com.tr/api/v1/me/plan \
  -H "Authorization: Bearer $PAYVEN_TOKEN"
```

## Yanıt

`200 OK` — `MyPlanDto`:

```json
{
  "plan": {
    "id":            "abc-...",
    "code":          "standard",
    "name":          "Standart Plan",
    "description":   "Sanal POS + Para Transferi modülleri",
    "price":         999900,
    "currency":      "TRY",
    "billing_cycle": "monthly",
    "features": [
      {
        "code":     "sanalpos",
        "name":     "Sanal POS",
        "category": "module"
      },
      {
        "code":     "para-transferi",
        "name":     "Para Transferi",
        "category": "module"
      },
      {
        "code":     "fraud",
        "name":     "Fraud Yönetimi",
        "category": "addon"
      }
    ]
  }
}
```

| Alan | Açıklama |
|---|---|
| `plan.id` | Plan kimliği |
| `plan.code` | Plan kodu (`free`, `standard`, `enterprise`, vb.) |
| `plan.name`, `description` | İnsan-okur ad |
| `plan.price` | Kuruş cinsinden (varsayılan TRY) |
| `plan.billing_cycle` | `monthly`, `yearly` |
| `plan.features[]` | Plana dahil feature'lar — `category: "module"` aktif modül, `addon` ek paket |

Tenant planı henüz atanmamışsa yanıt `null` döner.

## Aktif feature'ları öğrenmek

Plan'a dahil feature'ların **bu kullanıcı için aktif olup olmadığını** öğrenmek için [`GET /me`](/identity/auth/me) endpoint'ini kullanın — orada `features[].is_active` alanı kullanıcı + tenant + plan üçlüsünden filtrelenmiş halde döner.

## Yol haritası

- **Kullanım sayaçları** — bu plan kapsamında aylık transaction sayısı, RPS limiti, aktif merchant sayısı (henüz public response'da değil; backend hazırlıkta)
- **Plan yükseltme webhook'ları** — `plan.usage.warning`, `plan.usage.exceeded` (planda)
- **Self-service plan yükseltme** — şu an konsoldan veya satış kanalı üzerinden

Güncellemeler için: [Changelog](/resources/changelog).
